/**
 * Clase que enumera los tokens que se utilizaran en la aplicacion 
 * de acuerdo a los generado en el archivo lexer.flex.
 */

package Lexico;


public enum Token {
    VAR, INT, FLOAT, SUMA, RESTA, DIV, MULT, ASSIGN, ERROR, EXPO, PAREN_I, PAREN_F
}
